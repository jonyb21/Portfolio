{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Jon portfolio site with a small JSON-backed admin UI.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node tests/site.test.mjs",
    "eval": "node evals/preflight.mjs",
    "eval:browser": "node evals/browser-preflight.mjs"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  },
  "engines": {
    "node": ">=20"
  }
}
